function setup() {
    createCanvas(window.innerWidth, windowHeight);
}

function draw() {
    background(220);

    //Draw a rectangle 
    noStroke();
    fill(2, 64, 89);
    rect(0, 0, width, height);



}